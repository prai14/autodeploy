#while read ip password user;do echo "$ip";echo y|plink "$user@$ip" -pw "$password" -m syslog-add;done < end-ip
while read ip password user;do echo "$ip";echo y|plink "$user@$ip" -pw "$password" -m syslog-add;done < end
