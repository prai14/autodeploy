#while read ip password user;do echo "$ip";echo y|plink "$user@$ip" -pw "$password" -m magic;done < iplist
while read ip password user;do echo "$ip";echo y|plink "$user@$ip" -pw "$password" -m magic;done < iplist0
